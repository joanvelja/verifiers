# HF Singleturn

Generic Hugging Face single-turn environment. It normalizes arbitrary mapped
HF rows into Verifiers `prompt` / `answer` / `example_id` / `task` rows and
supports `mcq`, `math`, `open_ended`, and `exact` task types.
