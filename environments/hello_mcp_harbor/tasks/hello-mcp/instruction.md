# Hello MCP

An MCP server is registered with your tooling. It exposes a single tool
named `get_secret` that returns a secret string.

Your task:

1. Call the `get_secret` tool.
2. Write the returned secret value to `/app/secret.txt`.

Write the secret exactly as returned — no extra whitespace, no quotes, no
explanation. The file should contain only the secret string itself.
